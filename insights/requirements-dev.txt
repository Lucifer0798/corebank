-r requirements.txt

# Test-only dependencies. Same rule as requirements.txt: floors are what is actually in use, and
# every entry has a ceiling. These two had drifted furthest of anything here -- `pytest>=8.3` was
# resolving to 9.x and `pytest-asyncio>=0.25` to 1.x, both a major release past the pin, which is
# exactly the kind of jump that turns a green suite red on a commit that touched none of it.
pytest>=9.1,<10
pytest-asyncio>=1.4,<2

# Real Postgres for test_store.py, the same "verify against real infrastructure" practice the
# Java side uses in CoreBankTestcontainersIT -- store.py's SQL (the upsert, the GL exclusion, the
# create-database-on-missing-catalog path) is exactly the kind of logic a mock would let through
# wrong.
testcontainers[postgres]>=4.15,<5
